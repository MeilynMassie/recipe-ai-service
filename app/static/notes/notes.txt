Python Version = Python 3.12.14

AWS EC2
    1. Config instance
    2. Run ec2-setup.sh
    3. Test connection with Spring Boot app

To Run Ollama
    1. source .venv/bin/activate
    2. ollama serve
    3. python3 -m app.service.url_recipe_extractor  

To Run FastAPI
    1. python3 -m uvicorn app.main:app --reload --port 9000

TODO
    [ ] Clean up/refine LLM prompt
        [ ] Need to fix special characters when added to json
            "description": "In a large bowl, combine ground beef, soy sauce, sesame oil, chopped garlic, ginger, and scallions. Mix until combined. Optionally, cover and let marinate in the fridge for a couple hours. No worries if you skip this step, but if you have the time I recommend it since it&#039;ll make the filling more flavorful."
        [ ] Check why section is always returning null
    [ ] Test responses
    [ ] Add source to be url from POST instead of trying to find it in HTML